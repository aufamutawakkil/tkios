import {StyleSheet} from "react-native";

export const commonStyle = StyleSheet.create({
    input : {
        borderWidth:1,
        borderColor:'#bbb',
        padding:17,
        margin:5,
        fontSize:16,
        borderRadius:2
    },
    container: {
        padding: 10,
        backgroundColor: '#fff',
      },
});